# Server-side only — never prefix these with NEXT_PUBLIC_.
FASTAPI_BASE_URL=http://localhost:8000

# Must match api/.env's API_SERVICE_KEY value.
API_SERVICE_KEY=

# NL stats search BFF route (app/api/search) — server-side only.
ANTHROPIC_API_KEY=
